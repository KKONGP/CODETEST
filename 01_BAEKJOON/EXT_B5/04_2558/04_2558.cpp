#include <iostream>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int nA(0), nB(0);

    cin >> nA;
    cin >> nB;

    cout << nA + nB << '\n';

    return 0;
}